# Deferred verb record — `lehavot` / להוות

**Status:** `deferred_pending_authoritative_linguistic_source`

**Repository:** `idonokurasani/MindTune-Lab` (`feat/mantra-engine-phase1`)

---

## Target identity

| Field | Value |
|-------|-------|
| `verb_id` | `lehavot` |
| Target lemma | `להוות` |
| Intended pointed infinitive | `לְהַוּוֹת` |
| Plain TTS text | `להוות` |
| Root | `ה-ו-ה` |
| Binyan | `HIF'IL` |

## Reason for deferral

No approved pointed conjugation, phonology, stress indices, or Italian gloss
for the HIF'IL root `ה-ו-ה` lemma `להוות` is currently available in the
repository.

The previous `data/hebrew/specifications/v1/lehavot.json` was a copy of the
reviewed `lihyot` / `להיות` (PA'AL, root `ה-י-ה`) specification and was
removed because it could not be treated as an authoritative `להוות`
specification.

## Eligibility

This record is documentation only. It is **not** an approved specification, is
not loaded by `HebrewSpecificationRepository`, and must not be used for asset
generation, readiness evaluation, verb selection, or runtime execution until a
complete, reviewed linguistic source is supplied.
